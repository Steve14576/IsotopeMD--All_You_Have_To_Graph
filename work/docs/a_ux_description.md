1. 我要打mcm，我发现有很多分立的小点，我点击自由文本框命令新建自由文本框，打字。自由文本框大小由两个对角点决定，过了的自动换行与向下滚轮。边界可以缩放。我把新建三个小问的文本框并做了很多黑板工作。它们是文本框和箭头互相指。何谓正式与非正式，正式与非正式是否牵扯到块，变量，字符串，文本框，不知道，反正看能从这里看出多少思路。以往的drawio绘图或者mermaid绘图，我们现在直用我们这个画，我点击画框命令，画框，点击文本框命令，画文本框。它们都有gumball完成平移和缩放，且目前没有程序上的嵌套关系，箭头也没有程序上的指向关系，就像真正的黑板，语义通过空间本身表示。画完这个死黑板图后，我框选它并复制粘贴下来，以防损伤原件。复制粘贴的整个副本，我会想框选，正式定义一次，此时前端控件开始根据包络关系计算从属关系，正式定义内外，箭头指向。此时刚才的悬空箭头不会报错而是有提示，且其余部分不受影响。此时我想数据流向
2. 我要逆向一个化工流程题，并写结课报告，只有残破的几个拼起来也不完备的图，此时ai agent可以开始图搜索,而这个化工领域预制图块可以对它来说当成仿真引擎。
3. 我要做科研，和agent树状对话，启发式对话，此时可能要用成品且有一定规则的块，因为它们可以开新的块，开新的子树。可以有subagent分发任务，可以有coagent总结主旨进行翻译生成下文建议路径并充当和我答疑的不干扰主agent的秘书。不同分支可以debate，可以merge。分支之间可以clone，比如保留所有user回答，clone一段分支，并在agent回答换源并从头跑一遍，期间原user回答自动注入。对于开源且上下文可自定义的agent工具，比如opencode或者pi可以在节点划一段直接开始挂agent工具，可视化分支，剪枝，debate，clone，merge。在整个多树里面，每一节往下游都可以正式定义为一个正式agent实例，可原始agent，可opencode第三方，有分支则多态继承。agent可以被授予权限，执行新建agent，单步生图等的元操作，并有脚本级限额，防止抽风新建subagent把钱烧完。

