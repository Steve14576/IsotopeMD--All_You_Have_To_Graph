# 

*Exported on 2026年8月26日 17:16:16*

**User**:

然后这是我和我的coding agent说的，你怎么看：然后我们现在明确一下这个"正式定义"或者说“抓块”到底是什么意思，什么叫正式。什么叫正式块。这里面涉及到一堆很感性的第一性，不一定正交且全面，且可能会冲突。建议你列进去然后开展。首先，抓，是我在说框选一块有准备定义的字有准备定义的变量的非正式区域时，根据gui位置判断语义，吸附，对齐，成一个完整的，正式的电池块。它前端显示为md，后端怎么性能怎么来。后端从散的未定义到正式定义应该需要数据重排。这有一个问题，是否禁止块的嵌套，我只抓一些数字为一个列向量算不算一个块，还是说只是一个块的端口或者正式变量。我们的变量应该也要正式声明吧。我们的边是一等公民吗，从注册全局变量来说，答案是“是”吗？还有，aspen里面传一个流股的时候，是多维的，不是一维的，我们的变量不太适合一根一根接一大堆平行线吧。我们的变量是不是也要允许嵌套，那它们是块吗。变量的多维嵌应该是挺常见的一个功能吧。这还涉及到一个问题，正式块和正式变量是否是同一个东西，怎么有点串了。我框选选中一堆散的其中里面有我想定义为变量的文本框地抓，我这是抓块吗。我只抓一个文本框但是其内部有我想定义的变量，我这是抓块吗。我抓很多或者一个完全没有我想定义为变量的文本框，我这是抓块吗。抓块的哲学究竟是什么，我觉得我们以及开始有点乱了。怎么样叫抓，点选或框选多文本框，还是同一个文本框=但是里面有变量，甚至二者混合的更复杂的情况？可能要提供一些辨析或者统一退阶到语义明确的方式。我想到的是tovar的功能，统一转之前先声明一下哪些是要预备要转成变量的，通过点选多选，框选，光标框选都可以，甚至加一些智能识别，鼠标指过去时，其所在token选中，但是这又有问题就是为什么不直接转成正式变量。同时这还涉及到非正式文本框要不要算作同一个，要不要多开文本框，多开文本框看起来像手写黑板，那它在电脑上的哲学究竟是什么。这些在后端要算做同一个文本框吗。我更偏向于，正式块是很多正式变量绑定起来打的一个可以有文字注释的组，且我建议打组以后自带一个可正可负的存量变量数组来缓冲。同时，我发现至此，“抓”块的逻辑已经强依赖于gui了，像CAD那样了，这会不会削弱鲁棒性，屏幕兼容问题什么的。但是那是比较发散的情况了，就像在真正的黑板上头脑风暴一样，你不会抱怨黑板缩放变形。那么对标xmind和grassopper的我们应该也要能可以直接插入预制块，硬接线，比如就是在做类似orange的管道明确的工作的时候。到这里还说明了一个点，我们可以一开始就插入正式块，像一个个便利贴一样，到最后再定义变量定义端口然后硬接线了，那么之前的散装黑板文本框，后期再抓为块的，的更像一个独立的专门的创意功能，就像很少有人抱怨手机CAD为什么画不准或很难用一样。onshape应该是个例外，我也不知道它们怎么在手机且网页里面做到gui准的。那在这个意义上，我们是不是一直为了这个具体的gui位置语法的前端在斟酌我们的后端呢，那我们后端硬接线的，能和它解耦吗，自己先健康的跑起来吗，后端负责性能啊，应该是前端迁就后端而不是后端迁就前端不是吗，如果这个后端因为主动兼容前端这个具体的gui逻辑，它会不会变得又敏感又脆弱，像solidworks的恐龙时期windows图形api一样，后端竟然强绑定gui。

---
**Assistant**:

> The user wants me to review their message to their coding agent about "formal definition" / "grabbing blocks" and related architectural questions. This is a design review / clarification request. I sh...

这段话**方向对、问题提对了**，但作为给 coding agent 的指令**还不该这么发**——它会把感性冲突原样写进代码，而不是先消解冲突。我帮你把“抓/正式/块/变量/边/GUI 绑定”拆开钉死，再给你一份可以原样贴给 agent 的短规约。

---

## 一、先承认：你现在乱，是因为把四件不同的事叫成了同一个“抓”

你列的场景其实是**四种操作**：

| 你的动作 | 你在问的 | 真正该叫什么 |
|---|---|---|
| 框选散落文字，按位置猜输入/输出，吸附成一个带口的盒子 | 空间语法 → 封装 | **promote-to-block**（打组/封装） |
| 在一段字里点中 `Q` 或 `3.5`，声明它是变量 | 给内容起类型契约 | **promote-to-var**（声明变量） |
| 已经正式的块上再接线、改端口 | 图操作 | **connect / bind-port** |
| 从面板拖一个预制电池进来 | 跳过草稿 | **insert-prefab** |

**“抓块”不是一种原语，是几种原语的口语混称。**  
哲学一旦混，后端就会跟着混：变量变成块、块变成端口、边变成全局变量。

**统一退阶（建议你立刻采用）：**

```text
内容层永远是 Block（一块可编辑 md，可多开，像便利贴）
语义层永远是 Object（kind 开放：var / process / group / …）
端口是 Object 上的契约，不是独立“东西”
边是一等公民（有 id/type/props/status/delay），但不是变量
```

**正式 ≠ 块。正式 = 语义层里有一条记录。**

- 正式变量：`kind: 'var'` 的 Object，引用某段文本（或没有文本，纯手填）
- 正式块/电池：`kind: 'process' | 'group'` 的 Object，**绑定一组正式变量当端口**，可选注释文本
- 非正式：只有 Block，没有 Object

这样三个问题立刻有答案：

1. **“只抓一列数字当向量”** → 这是 **tovar**（一个 var，dtype=vector），**不是**一个电池块。除非你同时给它套了输入输出契约，才是 process。
2. **“框一堆散字，里面有我想当变量的”** → 先 tovar 那些 token，再 **promote-to-block** 把已声明的 var **绑成端口**。两步，不要一步魔法。
3. **“框一堆完全没有变量的字”** → 只是 **group 注释便利贴**（视觉组），或拒绝 promote，提示“先声明至少一个端口/变量”。不要假装它能跑。

**不要“为什么不鼠标指过去就直接转正式变量”**：预选（hover token）是**候选**，回车才是**声明**。直接转会把错选、单位、注释全变成图节点，图会脏死。

---

## 二、正式块 vs 正式变量：不要做成同一个东西

你后半段自己已经摸到正确答案：

> 正式块 = 若干正式变量绑在一起的、可带注释的组，建议自带可正可负的存量缓冲

这句话里有两个该保留、一个该改名：

**保留：** 块是**绑定关系**（哪些 var 是这个过程的 in/out/state），不是“一块更大的 md”。  
**改名：** 别叫“块=组=变量数组”。否则嵌套变量、嵌套块、端口会再次串台。

建议对象种类只保留这几档（正交）：

```text
Block          文本便利贴（可多个，哲学就是黑板碎片，不要合并成一个大 md）
Var            有类型/单位/值/status 的量（允许嵌套：record / vector / stream）
Process        有端口契约 + 可选 eval 的电池
Group          纯视觉组合，不参与求值
Subgraph       有对外端口的封装，求值可见
Edge           端口到端口的一等连接（可 delay、可挂 props）
Buffer/Stock   Process 或 Subgraph 上的可选存量（你说的正负缓冲）——是 Var 的一种角色，不是新物种
```

**变量嵌套 ≠ 块嵌套。**

- 流股多维（Aspen）：**一个 Var，dtype = record/stream**，`{T, P, F, x[]}`，**一根边传一整个记录**。禁止“温度一根线、压力一根线”当默认。
- 需要拆字段时：边上或下游用 **field 选择器**（Orange 的 channel 映射），不是平行边森林。
- 块嵌套：只有 Group/Subgraph 的 parent 指针。Var 嵌套走类型系统（record 字段），**不要**让 Var 的 parent 表示“几何上在哪个盒子里”——几何归属是 Block/Group 的事。

**边是不是一等公民？**  
数据结构上：**是**（id、属性、delay、status）。  
语义上：**边不是变量，也不是全局注册表。**  
“注册全局变量”是另一条通道（命名查找），和边正交。默认仍是**显式连线**；全局名是可选的 `alias`，否则图不可读。

---

## 三、多开文本框：后端不要合成一个

哲学：电脑上的“手写黑板”= **空间上的多张便利贴**，不是一篇 Word。

后端：

- 每个文本框 = 一个 `Block`，有自己的 id 和 rect
- **不要**为了“好存”合并成一个大 md
- 跨框框选 = 多 Block 上的多个 range + 一次命令里批量 tovar / 一次 promote-to-block

“算不算同一个文本框”：**永远不算。** 同一只是用户视觉上靠近，靠近用吸附/对齐处理，不用改数据模型。

---

## 四、GUI 位置语法会不会绑死后端——会，除非你把“猜测”关在命令参数里

你的担心完全对：若后端按像素猜“左边是输入”，就会变成 SolidWorks 绑 Windows GDI 那种脆。

**解耦法则：**

```text
前端：几何 → 猜一个 Intent（left=in, right=out, 吸附、对齐）
     → 生成一条 Command（带明确的 ports[] / vars[] / layout）
后端：只执行 Command，绝不读坐标、不读缩放、不读 DPI
```

后端健康自测（你要求 agent 必须先有这个）：

1. 零 GUI：纯 API 插入 prefab、声明 var、connect、跑 tick
2. 同一套图用 JSON 加载，坐标全改成乱的，**求值结果不变**
3. 手机/小窗：位置猜测可以更蠢，用户改 chip 确认即可；后端无感

**预制块硬接线**和**黑板抓取**是两条入口，汇合到同一 Command API。  
抓取是创意功能；insert-prefab 是 Orange/GH 工作模式。两条都要，**后端只认第二条的形状（结构化命令）**。

Onshape 能在网页里准，是因为约束求解在模型里（尺寸、重合），不靠像素。你以后若要“准”，加的是**约束/吸附作为生成 Command 的辅助**，不是把像素写进 schema。

---

## 五、这段话给 coding agent 的问题

优点：冲突都摊开了，比假装正交强。  
缺点：

- 没有裁决，agent 会实现“全都要”然后互相打
- “后端从散到正式要数据重排”——危险。**不要重排散文本**；只是**新增 Object 引用**原文。重排是空格键触发布局，不是 promote
- “抓已经强依赖 GUI”写成了既成事实，agent 会把 `x,y` 写进求值器

---

## 六、可直接贴给 agent 的短规约（建议替换你那段长独白）

```text
术语（禁止混用）：
- Block：一块 md 便利贴，可多开，永不自动合并
- Var：正式变量（Object, kind=var），可嵌套类型（scalar/vector/record/stream）
- Process：正式电池，端口=绑定的 Var，可选 stock/buffer（也是 Var）
- Group：视觉组，不影响求值
- Edge：端口间一等连接，一根边传一个值（值可以是 record/stream）
- 正式 = 语义层有 Object；非正式 = 只有 Block

原语（不要做一个万能“抓”）：
1. tovar：选中 token/框/多选 → 创建 Var，引用文本；未确认只高亮候选
2. promote-process：把已有 Var（或空端口）绑成 Process，布局只写 Command.layout
3. insert-prefab：跳过草稿，直接正式
4. connect(fromPort, toPort, {delay?})
5. group / ungroup

后端铁律：
- 求值器、存储、status 传播禁止读取像素/DPI/缩放
- 不因 promote 重排或改写用户 md；只加引用；anchor 失效 → orphan
- 无 GUI 时 API 必须能建图并跑 tick
- 流股默认 record 一根线，禁止默认平行标量线

第一期只做：
tovar + insert-prefab + connect + 单层 Process + record 类型
不做：自动位置成块、无限智能识别、变量与块合一、pickle 式重排
```

---

**一句话：**  
正式不是“看起来像电池”，而是**语义层多了一条可引用、可求值的记录**；抓是**人用 GUI 生成这条记录的多种手势**，不是后端概念。先让后端只吃 Command 跑起来，GUI 语法再慢慢加——顺序反了才会又敏感又脆。

如果你要，我可以把 `tovar` / `promote-process` 的 Command 字段（含一次框选里混有已有 Var、空文本、跨多 Block）写成 JSON 例子，直接给 agent 当测试用例。

---
**User**:

回到第一性。假如一开始，不引入流股，只是想做一个ux更加顺手的drawio，用户会希望什么操作流，用户画像是什么，怎么操作？此时从“抽象概念”上开始引入“有向有权流股”呢，他要怎么样我们说“它现在有流了”呢，最基本的完备了，剩下的歧义和不当的操作空间我觉得可以用手册约束，就像没人疯狂地在sw里面把所有东西强行画在同一个零件的同一个草图内。我发现我们刚才可能在尝试通过操作语法的设计来引导用户避免这样的不当操作，但是过了又会变成古板的工业求解器。用户究竟分别会怎么做呢？有一天drawio开始被魔法变得更人性了，那用户会许愿期望怎样具体的变人性呢。有一天drawio开始有流了，用户会怎么做？有一天drawio开始可以先乱画然后框一下生成电池，或者用户的粉笔黑板开始被魔杖点一下就真的能跑了，而不是画好图在那里脑补层级结构和流股，用户会怎么许愿，怎么做。

---
**Assistant**:

> The user wants me to go back to first principles about UX. They're asking:

回到第一性：先当一块**更好用的纸**，再让纸上的箭头**偶尔变成真的流**。用户许愿的从来不是“更严格的类型系统”，而是**少转头、少点对话框、少在脑子里补全图上没写出来的东西**。

---

## 一、第一天：没有流股，只是更顺手的 draw.io

### 用户是谁（画像很窄，别假装全民）

不是“所有要画图的人”。是三类重叠的人：

1. **自己跟自己讲话的人**  
   把一团乱事摊开：人员、方向、算法、待办、因果。图是外置工作记忆，不是交付物。
2. **两三人同步的人**  
   屏幕共享或投屏，边说边改箭头。要的是“大家看见同一张活纸”，不是版本库工作流。
3. **从纸/白板过来的人**  
   习惯粉笔：先字、再圈、再箭头。痛恨“必须先选图形库再落笔”。

他们**不是**第一天就想当仿真工程师。若你用电池/端口教育他们，他们会关掉软件去用纸。

### 他们希望的操作流（几乎全是手、几乎没有概念）

| 意图 | 手怎么动 | 软件不该要他们先懂什么 |
|---|---|---|
| 写下一点东西 | 双击空白 → 打字 | 节点类型、图层 |
| 再写旁边一点 | 再双击，另一张便利贴 | “是否同一文本框” |
| 圈起来 | 框选 → 松手就是一组视觉上的一坨 | Group 的形式化定义 |
| 连一下 | 从边拖出一条线到另一坨 | 端口、类型 |
| 改关系 | 拖线的头、改线上几个字 | 边对象模型 |
| 挪一坨 | 拖组，线跟着走 | 约束求解 |
| 弄整齐 | 偶尔按一下对齐/网格 | 自动排版宗教 |
| 找刚才 | 撤销、复制、框选多选 | 命令总线（他们不在乎名字） |
| 带走 | 一张图一个文件，打开还在 | 三层架构 |

**完备的 draw.io 人性化，到此就够了。**  
没有流、没有正式、没有电池。箭头只是**带字的有向线**：可以弯、可以粗、可以写“因为”“下周”“谁顶班”。权就是线的标签和粗细，还不是物理量。

### “更人性的 draw.io”用户会许什么愿（具体）

他们不会许“渐进形式化”。他们会许这些**可演示的小事**：

- 打字就是打字，不要先选矩形再双击再进编辑模式。
- 线从形状边缘长出来，不要先点连接点小十字。
- 框选一堆字和框，**默认当成“这一坨一起走”**，不要问“创建容器还是组合”。
- 线上的字跟着线走，改字不用点属性面板。
- 复制一坨，线不断、字不丢。
- 无限画布，缩放不糊，手机上能看、能挪，不求在手机上画准（你说得对：没人抱怨黑板变形）。
- 一键“别那么丑”（对齐），但**绝不在我没按的时候自己跳**。

手册约束的不当操作：把整学期画在一张图、箭头交叉成毛线——**允许，像 SW 允许一个草图画整机**。软件最多温和提示“图很大了，要不要另开一页”，不禁止。

---

## 二、第二天：抽象概念上出现“有向有权流股”

不要从 GUI 语法引入。从用户已经在做的事**加一层可读的意义**。

### 什么时候我们说“它现在有流了”

当且仅当同时满足：

1. **方向是真的**：A→B 和 B→A 不是同一条线的两种画法，反向是另一条关系（或明确的双向）。
2. **线上可以挂一个值**（权）：数字、短标签、或以后的 record。空着也行，空 = 权未填。
3. **这个值可以沿方向被“读到”**：哪怕第一版只是“点 B 能看见上游写了什么”，人不用把线当装饰脑补。

**还没有流的：** 仅装饰箭头、仅“因为所以”的语文线、仅视觉粗细。  
**已经有流的最小完备：**  
`有向边 + 边上的一份载荷（可空）+ 下游能读到这份载荷`。

不必有仿真、tick、单位、类型。**“有流”= 关系从句子变成了可指向的量。**  
用户感知的一句话：

> “我点这个框，能看见谁流进来、上面写了啥；我改线上的数，对面能显示出来。”

这就够宣布：draw.io 长出了流。

### 有了流之后用户会怎么做（观察，不是规定）

他们**不会**先设计端口表。他们会：

1. 继续当 draw.io 用，偶尔给某几条线写上数字或“3人”“高温”。
2. 发现“改这边，那边字没变”——开始抱怨，这时才需要**传播**（仍可以极简：改边标签 → 下游文本里的引用更新）。
3. 平行线画烦了，自己把几条线捏成一条，线上写 `T,P,F`——这就是 record 流股的**用户发明**，软件事后承认即可。
4. 画出环（心情→效率→心情）。**允许成环。** 第一天“有流”甚至可以不跑环，只显示“这里有环，权是上一笔写下的数”。手册写：要真反馈去开“步进”。不要用禁环当引导。

不当操作空间交给手册：一百根平行线、一个框上五十条出入。像没人拦你在一个零件里画宇宙。软件提供**折叠/一根复合线**当糖，不强制。

---

## 三、第三天：魔杖——乱画之后框一下，或粉笔能跑

这是用户真正的许愿，拆成他们**手上会做的事**，不是我们的对象模型。

### 许愿 A：“先乱画，框一下变成电池”

用户心理模型是便利贴魔法，不是 CAD 特征树：

1. 黑板上已经有几坨字、几根箭。
2. 框住一坨，说“这是一件事”。
3. **期望立刻看到：** 一个盒子、原来的字还在当注释、箭在盒子边变成“进出的口”（口可以先没名字）。
4. **不期望：** 先声明变量、先选 kind、先对齐网格。错了就拆盒，字回摊上。

“电池”在用户嘴里 = **有进出的一坨**，不是求解器单元。  
完备到此：盒 + 边界上的进/出（来自穿过框的箭）+ 仍可当纸编辑。  
还没有 eval，也可以叫电池——**能接线的注释盒**。

### 许愿 B：“点一下就开始跑，别让我在脑子里补层级和流”

用户要的是**看见动**，哪怕蠢：

- 我给入边写了 `3`，出边或盒上出现 `3` 或 `未算`。
- 我按空格/步进，环上的数挪一格。
- 某条线没写数：下游写“空”，图其余部分照样动。

他们**不会**许愿工业求解器。他们会许：

- 别让我先定义才能画。
- 别让半成品不能看。
- 错了像擦粉笔，不要报错对话框锁死。
- 某一坨我想“就当计算器”时，再教我公式；其余永远是纸。

预制块（Orange/GH）是**另一天的许愿**：“我今天不想发明轮子，给我一个现成盒子丢上去接线。”  
和魔杖不冲突：一个从纸长出盒，一个从库扔下盒。用户两天都会用，看心情。

---

## 四、操作语法 vs 手册：你发现的那件事是对的

用语法逼人“正确建图”，终点就是古板求解器：每一步合法，但不敢乱画。

更接近黑板的约束层次：

| 层 | 软件做什么 | 例子 |
|---|---|---|
| 物理上不能不做的 | 很少 | 边必须有两个端（或明确悬空）；文件能存开 |
| 默认善良 | 猜，可改 | 框选变组；穿过框的箭变成口 |
| 手册/文化 | 不禁止 | 别把整学期画一张图；别五十根平行线 |
| 可选严谨 | 开关 | 类型检查、单位、步进仿真 |

**引导用默认和糖，不用锁。** SW 一个草图画整机能打开——你的图一张纸画宇宙也必须能打开。

---

## 五、三条时间线上的“用户会怎么做”（对照表）

| | 更顺手的纸 | 纸上有流 | 魔杖能跑 |
|---|---|---|---|
| 第一分钟 | 双击打字、拉箭 | 同上，偶尔给线写个数 | 同上 |
| 感到爽 | 少对话框、线粘着走 | 改线，对面能看见 | 框一坨变盒，箭变口 |
| 感到烦 | 属性面板、先选图形 | 必须先端口才能连 | 必须先变量才能框 |
| 搞砸时 | 撤销、打散组 | 删线、清空权 | 拆盒，字回摊上 |
| 说“有了”的瞬间 | “这比 draw.io 少点两下” | “线不再是假的，对面能读到” | “我不用在脑子里跑这张图了” |

---

## 六、对你现在的产品含义（压回第一性）

本机自用、先 Web：

1. **先做完“人性 draw.io”**，用户画像按上面三类，操作流按第一张表。没有正式/抓/流。
2. **流的最小完备单独加一层：** 有向边 + 可空的权 + 下游可读。宣布“有流了”。
3. **魔杖最后做，且只做用户许愿的那一下：** 框选 → 盒 + 边界口（由穿过的箭生成）。变量声明是可选的糖，不是前置仪式。
4. 后端只保证：纸、线、权、盒、口能存、能读、能步进；**像素猜测只活在“生成用户可改的那一次动作”里**。

这样你不会再用操作语法冒充品德教育，也不会第一天就长成工业求解器。用户许愿的人性，全是**手少做决定、眼少做脑补、错了能擦**。

---
**User**:

额，但是我们对标的是grasshopper，simulink以及之前提到的那些工业求解器啊，或者说不是能力或者严谨性或者ux哲学对标，是至少完备性对标，然后想要什么自己造。还有，按照我们刚才的思想实验，这个“抓块”究竟要是怎样的才能不重复不乱呢，散的未定义文本框是在干什么，块是干什么，变量是干什么，谁让嵌套谁不让嵌套。以及我们刚才也说了，抓块是一个挺前端挺ux的东西，但是给agent快速出一个流仿真或者头脑风暴对话树，那么它们根本不需要gui。我们这个后端本身要能健康解耦，对吧？

---
**Assistant**:

> The user wants me to reconcile:

对。**完备性对标工业求解器，交互对标黑板**——这两件事必须拆开，否则你会永远在“抓块”里打架。Agent 不走 GUI，所以后端必须自己就是一台完整的图机。

---

## 一、完备性对标什么（能力清单，不是 UX）

对标 Grasshopper / Simulink / Orange / Aspen 草稿层，**最小完备**是这些能力都存在、且都能只通过 API 做出来（GUI 只是其中一种客户端）：

| 能力 | GH / Simulink 里对应什么 | 你的后端必须有 |
|---|---|---|
| 有向图 | 线、信号线 | Node + Port + Edge |
| 边上有载荷 | 数据树 / 信号 | Edge 传一个 Value（可为 record） |
| 复合载荷 | 一项里很多通道 | `dtype: record \| stream \| vector`，**一根线** |
| 成环 + 时间 | Unit Delay、反馈 | `edge.delay` + 全局 tick |
| 手填打断依赖 | 冻结、Constant | `status: manual` |
| 残缺仍能部分跑 | 部分失效变橙/红 | `undefined` 传染，不整图锁死 |
| 封装复用 | Cluster / Subsystem | Subgraph + 对外端口 |
| 纯视觉整理 | Group（不影响算） | Group，求值不可见 |
| 嵌套类型 | 树/总线 | Var 的类型嵌套，不是几何嵌套 |
| 插件电池 | 组件库 | `kind` + `eval` 契约，insert-prefab |
| 外部计算 | GhPython / S-Function | 以后 `eval: python`，现可不实现 |
| 历史/过程 | 示波器、数据记录 | tick 环形缓冲（可后做） |

**刻意不对标的：** 工业求解器的收敛、网格、单位制执法、形式化验证、必须先定义再连线。  
完备 = **这些动词都有**；严谨 = 用户自己造电池或手册约束。

Agent 建“流仿真 / 对话树”走的就是上表全部动词，**零像素**。

---

## 二、四个东西，禁止再混名

| 名字 | 是什么 | 嵌套？ | 有没有 GUI 也能存在 |
|---|---|---|---|
| **Block** | 一块 md 便利贴（内容层）。散的未定义文本框 = **只有 Block** | 空间上多开，**数据上永不合并**；不形成求值树 | 能。Agent 可 `createBlock(text)`，也可完全不创建 Block |
| **Var** | 一个有契约的量：名、类型、值、status | **类型可嵌套**（record 里再 record）；**不是**盒子套盒子 | 能。`createVar({name, dtype, value})` 可不绑文本 |
| **Process** | 电池：端口 = 绑上去的 Var + 可选 eval | 不“套 Var”；Var 是端口引用 | 能。`createProcess({ports, eval})` |
| **Group / Subgraph** | Group=视觉盒；Subgraph=有对外口的真封装 | **只有这两种允许几何/结构嵌套** | 能。纯 id + parent |

**正式 = 语义层有 Object（Var 或 Process 或 Subgraph）。**  
Block 永远可以非正式。  
**块 ≠ 变量。** Process 绑定 Var；Var 不是 Process。

散的未定义文本框在干什么？  
**只做人的外置记忆。** 不进求值。Agent 头脑风暴若只需要图，可以一个 Block 都没有。

---

## 三、“抓块”怎样才不重复不乱

**抓不是后端原语。** 后端只有：

```text
createBlock / editBlock
createVar   （可选 bind 到 Block 的一段 anchor）
bindPort    （Var → Process 的 in/out/stock）
createProcess
createGroup / createSubgraph
connect
```

前端“抓”是**手势宏**，展开成上面几条命令。宏可以笨，命令必须干净。

### 三种手势 → 固定展开（禁止一个万能抓）

1. **tovar**（点 token / 框数字 / 多选）  
   → 只 `createVar` + anchor。  
   **不**创建 Process。一列数字 = 一个 `dtype: vector` 的 Var。

2. **promote-process**（框一坨已经有箭或已有 Var）  
   → `createProcess` + 把穿过框的边收成端口 + 把框内已有 Var bind 到端口。  
   框内纯注释 Block 仍是注释，不自动变 Var。

3. **insert-prefab**  
   → 直接 Process + 端口，无 Block。Agent 默认走这条。

**禁止：** 一次手势既发明 Var 又发明 Process 又重排 md。  
要两下就两下。乱来的空间用手册说“先标量再装箱”，不要用锁。

嵌套规则一句话：

- **值**可以深嵌（流股 record）  
- **图结构**只有 Subgraph/Group 嵌套  
- **Var 不嵌套 Process，Process 不嵌套成 Var**  
- Block 不嵌套 Block（只有空间叠放）

---

## 四、后端如何自己健康（给 Agent、给以后的 GUI）

自检标准——**没有浏览器也能跑通：**

```text
createVar T, k
createProcess heat, ports {T,k} → q
connect …
set manual T=300
tick × N
assert q 有值或 undefined，图不崩
```

对话树同理：节点是 Process（或 kind=`utterance`），边是有向有权（权重=分支条件，可先当字符串），tick 或显式“走一步”。

GUI 位置猜输入输出 = **只写在 Command.layout / Command.guess 里**，求值器、存储、status **禁止读坐标**。  
这样：前端再 CAD 化，后端仍是工业求解器那张能力表上的一台图机。

---

## 五、和“先人性 draw.io”怎么并存

不矛盾，是**两层产品、一层内核**：

```text
内核（现在就要健康）：上表全部动词的 API + 存储 + tick
客户端 A：人的纸（可以暂时只用 Block + 无端口的箭）
客户端 B：魔杖（手势 → 命令）
客户端 C：Agent / 脚本（直接命令，insert-prefab）
```

完备性在内核；人性在客户端 A/B。  
工业求解器的“想要什么自己造”= 新 `kind` + 新 `eval`，不改内核。

---

**一句话：**  
散文本是纸；Var 是量；Process 是可跑的盒；抓是前端宏。  
对标 GH/Simulink 的是**内核动词齐**，不是抓的手感像它们。  
Agent 只打内核；GUI 不准把像素写进内核——解耦是否干净，用“无界面跑通 tick”一票否决。

---
